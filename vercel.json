{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",// Specify which paths will route to a destination using a regex
      "dest": "index.js" // Specify the paths' destination
    }
  ]
}